import React from "react";
import styled from "@emotion/styled";

const DDMenu: React.FC = () => {
  return <Menu data-test="ddmenu-component">...</Menu>;
};

export default DDMenu;

const Menu = styled("div")``;
