import { lightBlue, darkBlue, yellow } from '../styles/colors'

export const darkTheme = {
  colors: {
    navbarColor: `${lightBlue}`,
    backgroundColor: `${darkBlue}`,
    blogBar: `${yellow}`,
    sectionTitle: 'white',
    sectionTitleAccent: `${yellow}`,
    aboutText: 'white',
    aboutTextAccent: `${yellow}`,
  },
}
